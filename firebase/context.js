import React, { createContext } from "react";

//firebase context to make it avaliable for all react components in the app
const FirebaseContext = createContext(null);

export default FirebaseContext;
